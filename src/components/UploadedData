interface UploadTwoProps {
  data: ExtractedData;
  imageUrl: string | null;
}

const UploadData: React.FC<UploadTwoProps> = ({ data, imageUrl }) => {
  return (
    <div className="p-6">
      <h2 className="text-xl font-bold mb-4">Extracted Data</h2>
      
      {imageUrl && (
        <div className="mb-4">
          <img src={imageUrl} alt="Uploaded" className="max-w-sm rounded shadow" />
        </div>
      )}

      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {Object.entries(data).map(([key, value], index) => (
          <div key={index} className="bg-white/10 p-4 rounded text-white">
            <strong>{key}</strong>: {value}
          </div>
        ))}
      </div>
    </div>
  );
};